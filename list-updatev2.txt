SEKARANG TAMBAHKAN 3 FITUR BARU INI:

============================================================
FITUR 1: SEARCH DI STRINGS PAGE
============================================================
- Tambahkan search bar di atas list strings
- Filter real-time saat user mengetik
- Highlight kata yang cocok di hasil
- Case-insensitive search
- Tampilkan jumlah hasil yang ditemukan

============================================================
FITUR 2: AUTO DETECT PATCH DI PATCH EDITOR
============================================================
Buat fitur yang otomatis scan binary dan rekomendasikan patch:

Deteksi otomatis:
- Login bypass: cari string "Wrong", "Invalid", "Login Failed", "Error" → cari branch di sekitarnya (JNE/JNZ/CBNZ) → sarankan patch jadi JMP/JMP
- License check: cari panggilan strcmp/strncmp/memcmp → sarankan patch return 0
- Time check: cari time()/gettimeofday() → sarankan bypass
- Anti-debug: cari TracerPid check → sarankan patch

Tampilan UI:
- List semua deteksi dengan alamat + pattern
- Tombol "Patch" per item
- Tombol "Patch All" sekaligus
- Tampilkan status (success/failed) setiap patch

============================================================
FITUR 3: AUTO SEARCH OBFUSCATED STRINGS DI DEOBFUSCATE
============================================================
Fitur yang scan seluruh string di binary dan deteksi mana yang di-obfuscate:

Deteksi otomatis:
- Base64: pola [A-Za-z0-9+/]{10,}={0,2}
- Hex: pola [0-9A-Fa-f]{16,}
- XOR: entropy tinggi (random bytes) + coba brute force key 0x00-0xFF
- Unicode: pola \uXXXX
- URL Encode: pola %XX
- Reverse: string terbalik (deteksi dari pola)
- Shell: pola $'\xXX' atau \xXX

Tampilan UI:
- List semua string yang terdeteksi
- Tampilkan raw string + hasil decode
- Tombol "Decode" per item
- Tombol "Decode All" sekaligus
- Progress bar saat proses decode
- Export hasil ke file TXT

============================================================
FITUR TAMBAHAN (BONUS):
============================================================
A. Batch Deobfuscate — proses semua string sekaligus
B. One Click Crack — scan + detect + patch otomatis semua
C. History — riwayat file yang dibuka
D. Bookmark — tandai alamat penting
E. Export Report — hasil analisis ke TXT/HTML/JSON
F. Quick Action FAB — floating button akses cepat

============================================================
TEKNIS IMPLEMENTASI:
============================================================
- Kode Kotlin + Jetpack Compose
- Gunakan Coroutines untuk background processing
- StateFlow untuk UI state
- Room DB untuk history & bookmark
- Modular per fitur (UseCase pattern)
- Tambahkan loading indicator & progress bar

============================================================
LOKASI FILE YANG PERLU DIUPDATE:
============================================================
- app/src/main/java/com/oprek/tool/ui/screens/StringsScreen.kt
- app/src/main/java/com/oprek/tool/ui/screens/PatchScreen.kt
- app/src/main/java/com/oprek/tool/ui/screens/DeobfuscateScreen.kt
- app/src/main/java/com/oprek/tool/data/repository/FileRepository.kt
- app/src/main/java/com/oprek/tool/utils/PatternDetector.kt (baru)
- app/src/main/java/com/oprek/tool/utils/Deobfuscator.kt (update)

============================================================
OUTPUT:
============================================================
Berikan kode lengkap untuk ketiga fitur di atas (Search Strings, Auto Detect Patch, Auto Search Obfuscated). Sertakan juga kode untuk fitur bonus (Batch Deobfuscate, History, Bookmark) jika memungkinkan. Prioritaskan yang 3 fitur utama dulu.

TIDAK BOLEH ADA API KEY, TIDAK BOLEH ADA INTERNET, TIDAK BOLEH ADA AI/LLM. Semua 100% OFFLINE native Android.
